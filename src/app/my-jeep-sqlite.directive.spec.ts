import { MyJeepSqliteDirective } from './my-jeep-sqlite.directive';

describe('MyJeepSqliteDirective', () => {
  it('should create an instance', () => {
    const directive = new MyJeepSqliteDirective();
    expect(directive).toBeTruthy();
  });
});
